spring.application.name=com.patricio.springboot.app
server.port=8080

# DB Config
spring.jpa.defer-datasource-initialization=true

# --- NUEVA CONFIGURACI\u00D3N: Supabase (PostgreSQL) ---
# Reemplaza 'TU_PROYECTO_ID' con el ID que sale en tu URL de Supabase
# Reemplaza 'TU_PASSWORD' con la que creaste al inicio
spring.datasource.url=jdbc:postgresql://db.dzliqjoaxkfxddjfcrfv.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Alequiro154pqa.
spring.datasource.driver-class-name=org.postgresql.Driver

# Dialecto y JPA (Ajustado para Postgres)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Optimizaci\u00F3n para conexiones remotas (Supabase)
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=300000
spring.datasource.hikari.maximum-pool-size=10

# Log
logging.level.root=INFO
logging.level.com.example=DEBUG
logging.file.name=logs/spring-boot-app.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
spring.jpa.open-in-view=true